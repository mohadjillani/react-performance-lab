## What

<!-- One or two sentences. Which branch does this target, and why that one? -->

## Checklist

- [ ] `npm run build && npm run test:e2e` passes unchanged (behavioural parity)
- [ ] `npm run measure` passes this branch's budgets, or the budget change is explained
- [ ] `package.json` and `package-lock.json` are identical to `main`
- [ ] Harness changes are made on `main` and merged down the fix branches
